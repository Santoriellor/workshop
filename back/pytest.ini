[pytest]
# pytest-django is installed (requirements.txt) and seven test modules exist,
# but there was no pytest configuration of any kind, so DJANGO_SETTINGS_MODULE
# was never set and every module failed at collection. This file is the whole
# fix for that half of the problem.
DJANGO_SETTINGS_MODULE = backend.settings
python_files = test_*.py
